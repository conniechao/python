1. 注释：
单行：#abcdefg
多行：'''abcdefg'''

2. 区分大小写

3. 变量：
无需声明直接赋值，可动态改变
命名规则：使用标识符
         以字母、下划线开头（不能以数字开头），后跟字母、数字、下划线，字母可包含中文日文字符
         不能用关键字，可以包含
         不能用内置函数名，否则内置函数会被覆盖
         不能有空格

4. print：
print(a,b,c, sep='~'， end="")
输出结果默认空格间隔，可通过sep参数设置
end默认换行"\n"，end=""不换行

5. 关键字：
import keyword
keyword.kwlist

